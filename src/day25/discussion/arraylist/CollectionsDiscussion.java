package day25.discussion.arraylist;

public class CollectionsDiscussion {
    /*

    COLLECTIONS

    The Collection in Java is a framework that provides an
    architecture to store and manipulate the group of objects.

    Java Collections can achieve all the operations that you
    perform on a data such as searching, sorting, insertion,
    manipulation, and deletion.

    Objectives

    . Understand the purpose of the Collections Framework and why it is more
    powerful than basic arrays.
    · Learn how to use ArrayList for dynamic lists, including methods like add(),
    remove(), get(), and size().
    · Explore LinkedList and its node-based structure, practicing methods such as
    addFirst(), addLast(), removeFirst(), and removeLast().
    · Understand HashMap for storing key-value pairs, using methods like put(),
    get(), remove(), and containsKey().
    . Apply collections in practical scenarios like managing student lists.


    ARRAYLIST

    The ArrayList class is a resizable array, which can be found in the
    java.util package.

    The difference between a built-in array and an ArrayList in Java, is
    that the size of an array cannot be modified (if you want to add or
    remove elements to/from an array, you have to create a new one).
    While elements can be added and removed from an ArrayList
    whenever you want. The syntax is also slightly different:

    ARRAYLIST METHODS

    · Adding an Item
    add(element), add(index, element), addAll(), contains()
    · Accessing an Item
    get(index), indexOf(element), lastIndexOf(element)
    · Changing an item
    set(index, element)
    · Removing an item
    remove(index), remove(element)
    · Removing all items
    removeAll(obj), clear()

     */
}
