package day24.discussion.abstractionclass;

public class AbstractionDiscussion {
    /*

    Abstraction is a process of hiding the implementation and showing only
    functionality to the user.

    Another way, it shows only essential things to the user and hides the
    internal details, for example, sending SMS where you type the text and
    send the message. You don't know the internal process about the
    message delivery.

    Abstract class in Java

    A class which is declared as abstract is known as an abstract

    class in Java. It can have abstract and non-abstract
    methods. It needs to be extended and its method. It cannot be
    instantiated.

    Abstract class in Java

        Points to Remember
        . An abstract class must be declared with an abstract
        keyword.
        . It can have abstract and non-abstract methods.

        . It cannot be instantiated.

        . It can have constructors and static methods also.
        . It can have final methods which will force the subclass
        not to change the body of the method.

        Ways to achieve
        Abstraction

        . Abstract Class
        · Interface


        implements keyword

        An interface is declared by using the interface
        keyword. It provides total abstraction; means all the
        methods in an interface are declared with the empty
        body, and all the fields are public, static and final by
        default.

        A class that implements an interface must implement
        all the methods declared in the interface.


     */
}
